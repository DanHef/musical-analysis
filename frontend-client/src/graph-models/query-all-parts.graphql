query queryAllParts ($sessionId: ID!){
    analysisSession (id: $sessionId) {
        parts {
            id
            started
            stopped
            description
            user {username}
        }
    }
}