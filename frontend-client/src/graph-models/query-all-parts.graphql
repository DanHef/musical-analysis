query queryAllParts ($sessionId: ID!){
    analysisSession (id: $sessionId) {
        parts (paging: {offset: 0 limit: 50}){
            id
            started
            stopped
            description
            user {id username}
            tag {id name}
        }
    }
}