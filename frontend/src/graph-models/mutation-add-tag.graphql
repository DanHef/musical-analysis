mutation addTag($nameOfTag: String!, $tagDescription: String!) {
  createOneTag(input: {
    tag: {
      name: $nameOfTag
      description: $tagDescription
    }
  })
  {
    id
    name
    description
  }
}