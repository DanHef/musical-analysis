query AnalysisSessionTags ($analysisSessionId: ID!){
    analysisSession(id: $analysisSessionId)
    {
      id
      name
      started
      stopped
      tags
      {
        id
        name
        description
      }
    }
  }