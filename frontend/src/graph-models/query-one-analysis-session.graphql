query OneAnalysisSession ($analysisSessionId: ID!){
    analysisSession(id: $analysisSessionId)
    {
        id
        name
        tags {
            id
            name
        }
        started
        stopped
    }
}