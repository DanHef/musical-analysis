query OneAnalysisSession ($analysisSessionId: ID!){
    analysisSession(id: $analysisSessionId)
    {
        id
        name
        started
        stopped
        tags {
            id
            name
            description
        }
        parts {
            id
            started
            stopped
            tag {name}
            user {id username}
        }
    }
}